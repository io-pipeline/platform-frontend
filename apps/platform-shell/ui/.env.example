# Development Server Configuration

# Backend/web-proxy URL for Vite dev server proxy
# This is where the dev server will proxy gRPC/API requests
# Default: http://localhost:38106
VITE_BACKEND_URL=http://localhost:38106

# Vite dev server port
# Default: 33000
VITE_DEV_SERVER_PORT=33000

# gRPC/Connect Configuration

# Platform Registration Service URL (runtime)
# In production, this should point to the Consul sidecar for service discovery
# Default: same as application origin (window.location.origin)
# Example production with Consul sidecar: http://localhost:8500
VITE_PLATFORM_REGISTRATION_URL=

# Use binary protobuf format (recommended for better performance)
# Default: true
VITE_GRPC_USE_BINARY=true

# Enable debug logging for gRPC connections
# Default: true in dev mode, false in production
VITE_GRPC_DEBUG=false
